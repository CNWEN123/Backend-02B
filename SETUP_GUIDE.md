# 🚀 系统初始化与功能验证指南

## 当前状态

✅ **服务正在运行**: https://3000-i9mzigr2smruxfa8tgrk9-2e77fc33.sandbox.novita.ai  
⚠️ **数据库已重置**: 需要重新初始化数据

---

## 🔧 快速初始化步骤

### 方法1：使用已有数据（推荐）

由于数据库被重置，最快的方法是恢复之前的数据。

**如果您有数据库备份:**
1. 停止服务: `pm2 delete live-dealer-admin`
2. 恢复备份数据库文件到 `.wrangler/state/v3/d1/`
3. 重启服务: `pm2 start ecosystem.config.cjs`

###方法2：手动初始化（如果没有备份）

#### 步骤1: 执行数据库初始化SQL

在服务器终端运行：

```bash
cd /home/user/webapp

# 等待数据库文件创建（服务启动后自动创建）
sleep 3

# 查找数据库文件
DB_FILE=$(find .wrangler/state/v3/d1 -name "*.sqlite" -type f 2>/dev/null | head -1)
echo "数据库文件: $DB_FILE"

# 如果找到数据库文件，可以使用wrangler执行SQL
# 但由于没有sqlite3命令，我们需要通过API来创建数据
```

#### 步骤2: 通过浏览器创建管理员和代理

由于我们没有直接的SQL执行工具，**最简单的方法是创建一个临时的初始化页面**。

---

## 🎯 方法3：使用初始化API（最简单）

我为您准备了一个初始化端点。在浏览器访问：

```
https://3000-i9mzigr2smruxfa8tgrk9-2e77fc33.sandbox.novita.ai/init-system
```

这个页面会：
1. ✅ 创建管理员账号 (admin/admin123)
2. ✅ 创建测试代理（带邀请码和专属域名）
3. ✅ 创建测试用户
4. ✅ 初始化所有必要的数据

点击"初始化系统"按钮即可。

---

## 📋 验证新功能

初始化完成后，按照以下步骤验证新功能：

### 1. 登录系统
```
URL: https://3000-i9mzigr2smruxfa8tgrk9-2e77fc33.sandbox.novita.ai
账号: admin
密码: admin123
```

### 2. 进入代理管理
```
左侧菜单 → 层级管理 → 代理管理
```

### 3. 查看新功能

#### ✅ 应该看到的内容：

**代理列表页:**
- 表头有"分享链接"列（在"佣金"列和"下级/玩家"列之间）
- 每个代理显示8位邀请码（如：`A3B7C9D2`）
- 邀请码旁边有📋复制按钮
- 操作列有紫色🔗"分享设置"按钮

**点击"新增代理":**
- 表单底部有橙色背景的"专属域名绑定"区域
- 有专属域名输入框

**点击"分享设置"按钮:**
- 弹出完整的"分享与域名设置"弹窗
- 包含4个区域：
  1. 代理信息（灰色）
  2. 注册绑定邀请码（绿色边框）
  3. 专属分享链接（蓝色边框）
  4. 专属域名绑定（橙色边框）

---

## 🔍 如果还是看不到新功能

### 检查清单：

1. **强制刷新浏览器**
   - Windows: `Ctrl + Shift + R`
   - Mac: `Command + Shift + R`

2. **清除浏览器缓存**
   - 按 `F12` 打开开发者工具
   - 右键点击刷新按钮
   - 选择"清空缓存并硬性重新加载"

3. **使用无痕模式**
   - Chrome: `Ctrl + Shift + N`
   - Firefox: `Ctrl + Shift + P`

4. **检查JavaScript加载**
   - 按 `F12` → Console 标签
   - 输入: `typeof showAgentShareSettings`
   - 应该返回: `"function"`

5. **查看网络请求**
   - 按 `F12` → Network 标签
   - 刷新页面
   - 查找 `app.js` 文件
   - 确认状态码是 200（不是 304）

---

## 🆘 快速测试方案

如果您想快速验证API是否正常工作，可以在浏览器控制台（F12）运行：

```javascript
// 1. 测试公开API
fetch('/api/v1/public/agent-by-invite/TEST1234')
  .then(r => r.json())
  .then(data => {
    console.log('公开API测试:', data);
    // 应该返回: {success: false, message: "邀请码无效或代理已禁用"}
  });

// 2. 检查JavaScript函数
console.log('showAgentShareSettings:', typeof showAgentShareSettings);
console.log('saveAgentShareSettings:', typeof saveAgentShareSettings);
console.log('regenerateInviteCode:', typeof regenerateInviteCode);
console.log('verifyAgentDomain:', typeof verifyAgentDomain);
// 都应该返回: "function"
```

---

## 📞 需要进一步帮助？

如果以上方法都无法解决问题，请提供：

1. 浏览器名称和版本
2. 开发者工具Console中的错误信息（截图）
3. Network标签中app.js的加载状态（截图）
4. 代理管理页面的截图

---

**当前系统状态**: 
- ✅ 后端API已部署
- ✅ 前端UI已更新
- ✅ 数据库Schema已包含新字段
- ⚠️ 需要初始化数据

**预览地址**: https://3000-i9mzigr2smruxfa8tgrk9-2e77fc33.sandbox.novita.ai

**版本**: Live Dealer Admin V2.1  
**更新时间**: 2024-11-30
